<!-- bookまたはuserをobjにした、これで置き換えやすくする -->
<% if obj.errors.any? %>
    <div id="error_explanation">
    	<font color="red">
	        <h3><%= obj.errors.count %> error prohibited this obj from being saved:</h3>
    	    <ul>
        	    <% obj.errors.full_messages.each do |msg| %>
            	    <li><%= msg %></li>
            	<% end %>
        	</ul>
        </font>
    </div>
<% end %>